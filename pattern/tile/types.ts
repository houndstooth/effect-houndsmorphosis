import { Address, Unit } from '../../../../src'

interface HoundsmorphosisTileOriginParams {
	absoluteX: number,
	absoluteY: number,
	address: Address,
	baseSize: Unit,
	tileSize: Unit
}

export { HoundsmorphosisTileOriginParams }
